<div class="row">
  <div class="col">
    <% if @grand_parent.photos.attached? %>
    <% @grand_parent.photos.each do |photo| %>
    <div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url(<%= cl_image_path photo.key  %>);">
      <% end %>
      <% else %>
      <div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url(<%= cl_image_path "https://images.unsplash.com/photo-1447069387593-a5de0862481e?ixlib=rb-1.2.1&ixid=eyJhcHBfaWQiOjEyMDd9&auto=format&fit=crop&w=1350&q=80" %>);">
      <% end %>
      <div class="container">
        <h1><%= @grand_parent.first_name %></h1>
      </div>
    </div>
  </div>
</div>
<div class="d-flex flex-wrap">
  <div class="container w-50">
    <div class="card-product-infos">
      <h4><%= @grand_parent.description %></h4>
      <p>🌟<%= @grand_parent.hobby %></p>
      <p>🌟Ville:<%= @grand_parent.city %></p>
      <p>🌟Age:<%= @grand_parent.age %> ans</p>
      <p>🌟Prix:<span id="price_gp"><%= @grand_parent.price %></span> euros💶</p>
    </div>
  </div>

  <%= render "bookings/new", grand_parent: @grand_parent, booking: @booking %>
</div>

<div class="container d-flex justify-content-center">
 <p>
  <%= link_to 'Supprimer', grand_parent_path(@grand_parent), method: :delete, data: { confirm: "Are you sure?" }, class: 'btn btn-flat' %>
 </p>
</div>

<script>

const booking_date_start = document.getElementById('booking_date_start_3i');
const booking_date_end = document.getElementById('booking_date_end_3i');
const votre_resa = document.getElementById('votre_resa');
const grand_parent_price = parseInt(document.getElementById('price_gp').innerText, 10);
let x = 0;
let y = 0;

booking_date_start.addEventListener('change', (e) => {
  x = e.target.value;
  let total = (y-x)*grand_parent_price;
  if (total > 0) {
    votre_resa.innerText = `🌟 Le montant total de votre réservation est de ${total}€ pour ${y-x} jour(s).`;
  }
});

booking_date_end.addEventListener('change', (e) => {
  y = e.target.value;
  let total = (y-x)*grand_parent_price;
  if (total > 0) {
    votre_resa.innerText = `🌟 Le montant total de la réservation est de ${total} pour ${y-x} jours`;
  }
});
</script>

